{
    "model_version": "lstm",
    "lstm_num_units": 1,
    "embedding_size": 50,

    "learning_rate": 1e-3,
    "batch_size": 32,
    "num_epochs": 300,
    "dropout_rate": 0.3,

    "save_summary_steps": 100,

    "rnn_output_size" : 90
}
